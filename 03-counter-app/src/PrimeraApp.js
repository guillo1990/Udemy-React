import { Fragment } from "react";

const PrimeraApp = ({str, sec}) => {

    return (
        <>
            <h1>{ str }</h1>
            <p>{ sec }</p>
        </>        
    );

}

export default PrimeraApp;